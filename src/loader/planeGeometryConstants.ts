/**
 * Default plane mesh dimensions (must match `PlaneGeometry` args in createPrimitive / createShapeGeometry).
 * Full width and height are both `PLANE_GEOMETRY_MAX_EDGE` world units before entity scale.
 */
export const PLANE_GEOMETRY_HALF_EXTENT = 100
export const PLANE_GEOMETRY_MAX_EDGE = PLANE_GEOMETRY_HALF_EXTENT * 2
